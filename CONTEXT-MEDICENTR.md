# КОНТЕКСТ ПРОЕКТА — «МЕДИАЦЕНТР» (Сюжет-Студия)

> Бриф для продолжения работы другой нейросетью/разработчиком. Отражает состояние
> на 2026-09-11. Читать вместе с `README.txt`.

## 1. Что это

Веб-приложение для монтажа новостных сюжетов: сценарий с разметкой видео
(таймкоды In/Out), экспорт в Fish Cutter (CSV), Word, MOGRT-титры. Работает
локально: `server.py` раздаёт статику + общий список ФИО (`names.json`),
коллеги в той же сети открывают `http://IP:8765`.

- **Папка:** `/Users/Shared/Сюжет-Студия` (git-репозиторий, ветка не коммитилась).
- **Windows-сборка** перегенерируется: `./make-win.sh` → `../Сюжет-Студия-Windows`
  (bat/README/правки — свои, статика 1:1).
- **Стек:** чистый vanilla JS (без сборки, без npm), Python 3 stdlib сервер,
  один CSS. Файлы: `index.html`, `js/app.js` (движок), `js/octopus.js`
  (оболочка newsroom «МЕДИАЦЕНТР»), `css/style.css`, `server.py`, `names.json`.

## 2. Архитектура и связи (важно для правок)

- `js/app.js` грузится первым, `js/octopus.js` вторым (обычные `<script>`, НЕ
  модули; `"use strict"`). Верхние `const/let` — в lexical-скоупе, НЕ на `window`
  (кроме явно повешенных `window.OC`, `window.SS_HOOK`, `window.SS_IDB_OK`,
  `window.SS_LS_MIGRATED`).
- **Мост между файлами — `SS_HOOK`** (объект точек расширения в app.js:
  `afterRender, afterSave, afterVideoList, beforeSearch, draftMeta, draftLoaded,
  commentCount, showBlockComments`). octopus.js назначает свои функции. РАНЬШЕ
  был опасный monkey-patch (`renderBlocks = function(){...}`) — ЗАМЕНЁН на хуки.
  Новые пересечения функциональности добавляй через `SS_HOOK`, не переназначением.
- Движок самодостаточен без оболочки; оболочка рисует newsroom-слой
  (комментарии/задачи, переключение сюжетов через «Проект» → «Сюжеты…»,
  чек-лист выхода «Утверждение»).

## 3. Модель данных

- **Блок сценария** (`state.blocks`): `{id, kind, text, speaker, role, folded,
  partsFolded, parts:[{file, path, in, out}]}`.
  `kind`: `headline|vod|vo|sync|standup|life|spiegel`. `in/out` — секунды.
  **Ключи `kind` и английские значения `stage` не менять** — на них держатся
  сохранённые данные и триггеры набора. Видимые подписи — отдельно (см. §5).
- **Триггеры нового блока** (DOC_TRIGGERS): на новой строке «хед/лид/зк/синх/
  стенд/лайф/шпи» + пробел.
- **Сюжет** (`OC.stories`): `{id, title, day, time, stage, stageDates, targetSec,
  blocks, nextId, comments, modified}`. `comments`: `{kind, who, text, time,
  done, blockId}` — `blockId` якорит комментарий к блоку.

## 4. Что уже сделано (сессия 2026-09-10)

### P0 — баги и целостность данных
- **Таймкоды для дробных fps (29.97/59.94):** `tc()` теперь считает кадры от
  `Math.round(fps)` (NDF: 30/60 кадров в «секунде» таймкода). Был дрейф ~1с/100с.
- **Фрагменты хранят путь:** добавлен `parts[].path` (relPath). Гашение коллизий
  одноимённых файлов из разных подпапок. `normalizeBlocks` мигрирует старые
  черновики (path←file). В CSV Fish Cutter и MOGRT добавлена колонка «путь».
- **localStorage → IndexedDB:** сюжеты (`oc_stories`, `oc_active`), миниатюры
  (`posters`) и дескриптор папки (`kv`) переехали в IndexedDB (`IDB`-обёртка,
  БД `ss-studio`). При первом запуске — авто-миграция из localStorage с
  освобождением ~5 МБ. Фолбэк: если IDB недоступен (приватный режим) — тихий
  возврат к localStorage.
- **Квота не молчит:** `store.set` при переполнении сам выкидывает легаси-дубли и
  повторяет; если не помогло — баннер `#quotaBanner` с типом ошибки, байтами,
  топ-ключами и кнопками «Сохранить черновик»/«Снимок данных» (диагностический
  JSON с `SS_IDB_OK` и числом сюжетов из IDB). `evict` НЕ трогает `oc_stories`
  пока миграция не подтверждена (`SS_LS_MIGRATED`).
- **Гонки:** `persistStories` буферизуется до конца `ocInit` (проба IDB), чтобы не
  писать мегабайты в localStorage до очистки.
- **Безопасность `names.json`:** CSRF-защита (POST требует `Content-Type:
  application/json` + `X-Requested-With`), версионирование `_v` с `409 Conflict`
  (два пользователя не затирают друг друга), ETag+no-store, HTTP/1.1, блокировка
  `.git/__pycache__/*.bak/*.tmp`. Клиент подтягивает общий список при
  возврате на вкладку (`visibilitychange`).
- **XSS/санитайз:** проверка `meta` импортируемого черновика (`draftLoaded`),
  экранирование в выводах.

### P1 — функционал
- **Хронометраж-план (`targetSec`):** строка ИТОГО + кнопка «план мм:сс ✎»,
  переработка — красным.
- **Управление сюжетами:** ПКМ в MY ASSIGNMENTS и меню «Проект» → Дублировать /
  Удалить. Удаление активного не «дописывает» редактор в чужой сюжет.
- **Папка исходников:** File System Access API (`showDirectoryPicker`), дескриптор
  в IDB → ⟳ открывает без повторного выбора (Chrome/Edge). Фолбэк
  `webkitdirectory` для SMB.
- **Комментарии к блокам:** привязка `blockId`, маркер «💬n» в блоке, фильтр,
  переход «→».
- **Поиск по тексту блоков** (глобальный и в MY ASSIGNMENTS).
- **Миниатюры в IDB** (переживают перезагрузку). **Скорость 0.25–4× и громкость**
  в мониторе, клавиши J/K/L.

### P2 — UI и здоровье кода
- Модалки `ask`/`confirm2` вместо `prompt/confirm`, ловушка Tab (`trapTab`).
- **Степпер этапов** в шапке сюжета (клик — перевод, назад с подтверждением).
- Точечные обновления вместо `ocRefreshAll` везде; `data-rel` в списке файлов.
- `[hidden]{display:none!important}` — починен «висячий» баннер квоты.

### Ребрендинг и русификация (последняя правка)
> ⚠ Сессия 14.09 (Л1–Л4): хром интерфейса переведён на **EN-термины NLE**
> (вариант B: английские подписи панелей/полей + русские тексты сообщений),
> бейджи типов — снова интернациональные коды. Ниже — историческое состояние 11.09.
- Название **OCTOPUS → МЕДИАЦЕНТР** (title, шапка, комментарии). Технические
  имена (`octopus.js`, `oc*` id, ключи) НЕ менялись — иначе ломались бы данные.
- Интерфейс на русском: навигация, вкладки, секции, поля, кнопки типов.
  **Ключи `kind` и `value` фильтров остались английскими.**
- Краткие бейджи типов: **ЗАГ / ПОДВ / ЗК / СИНХ / СТЕНД / ЛАЙФ / ШПИГ**
  (в `KIND_META.badge`; экспорты в Word/CSV — полные русские имена как раньше).
- Этапы по-русски через `STAGE_LABEL` (ASSIGNED→НАЗНАЧЕН … ON AIR→В ЭФИРЕ);
  внутренние `stage`/ключи `stageDates` — английские.

### Блоки: ПКМ-добавление, drag-порядок, кнопка ⇄ (сессия 2026-09-11)
- Кнопки «+ Заголовок … + Шпигель» из `#scriptToolbar` **удалены** (как и класс
  `.tb-btn`). Добавление типа блока — **ПКМ по пустому месту** `#blocks`
  (`showAddKindMenu` в app.js: меню `.kind-menu`, вставка **в конец** через
  `addAndFocus`). ПКМ **внутри** `.doc-block` не перехватывается — работает
  нативное меню (копипаст в textarea). Окно «Сценарий» в octopus.js не затронуто.
- Дропдаун «сменить тип» **убран с бейджа**: клик по бейджу ничего не делает,
  бейдж — ручка **перетаскивания** порядка (`draggable` включается только на
  время `mousedown`, чтобы не ломать выделение текста; HTML5 DnD, `dragover` по
  половинам строки → `.drop-before/.drop-after`, сброс — `dragCleanup()`).
  Вставка — `moveBlockTo(id, to)` с `histBefore()` (Ctrl+Z откатывает). Drop на
  пустое место `#blocks` = в конец.
- Смена типа — кнопкой **⇄** в `doc-tools` (`data-act="kind"` → тот же
  `showKindMenu`, с подтверждением про фрагменты). Alt+↑/↓ и `DOC_TRIGGERS` —
  без изменений.
- **Двойной клик по бейджу** сворачивает/разворачивает блок (`folded` — view,
  не история); кнопка «▾» убрана из `doc-tools` как дубль, «⤡ все» осталась.
- **Курсив расшифровок**: `.doc-block.has-parts .doc-text { font-style: italic }`
  (СИНХ/СТЕНД/ЛАЙФ/ШПИГ); в Word-экспорте текст этих типов курсивом.
- **Экспорт Word — настоящий `.docx`** (app.js: `crc32` + `zipStore` (ZIP method 0,
  store) + `buildDocx` (OOXML: [Content_Types],rels,styles.xml,document.xml);
  `exportWord` → `…_для_редактора.docx`). Причина: HTML-подделка под .doc
  открывалась в Protected View и не сохранялась. **Выключка слева** во всех
  параграфах (`w:jc val="left"` в стилях и каждом `w:pPr`). `styleId` —
  `Heading1/Heading2` (под парсер импорта `/heading/i`). Старый `buildDoc` удалён;
  импорт старых HTML-`.doc` по-прежнему работает (parseWordHtml). Проверено:
  `unzip -t`, парсинг XML, чтение textutil, round-trip через логику parseDocx.
- Импорт Word — закрепление: `parseDocx` считает заголовками и `styleId` вида
  `H1`–`H6` (регулярка `/heading|^h[1-6]$/i`) — иначе не импортировались файлы
  промежуточной сборки 11.09, экспортированной до переименования стилей в
  `Heading1/2`. При нуле блоков `importWordFile` показывает тост (7 с) с первыми
  2 нераспознанными строками вместо безликого «нет знакомых заголовков».
  Бинарный .doc (OLE) по-прежнему не поддерживается — в README добавлена
  подсказка «пересохраните в .docx».
- **Возврат правок из Word**: Word при пересохранении **переписывает `styleId`**
  (`Heading1`→`"1"`, `Heading2`→`"2"`), человекочитаемое имя «heading 1» живёт
  только в `word/styles.xml`. `parseDocx` теперь читает и `styles.xml` (карта
  id→`w:name`, try/catch при отсутствии файла) и определяет заголовок по имени
  (`/heading|^h[1-6]$/i` + страховка `/^заголовок\s*[1-6]/i`). Проверено на
  живых файлах: свой экспорт (store) и возврат из Word (deflate + числовые
  styleId) — блоки распознаются.
- **Бейдж: клик = свернуть/развернуть** (dblclick убран; `e.stopPropagation()`,
  иначе пузырь по свёрнутой строке тут же развернул бы обратно). Клик по пустому
  месту **свёрнутого** блока тоже разворачивает (в общем обработчике клика `div`,
  до веток `data-act`). Реальный drag события `click` не порождает — порядок не
  страдает.
- **ПКМ внутри блока СИНХ/СТЕНД/ЛАЙФ/ШПИГ** (`showBlockCtxMenu`, `.proj-menu`):
  «🎞 Добавить фрагмент» → `pullPart(b)`; «Копировать выделенное» / «Вставить»
- **Фикс (13.09)**: пункты `showBlockCtxMenu` жили на `onclick`, а `mousedown`
  на document закрывал меню раньше клика — действия не срабатывали. Переведены
  на `onmousedown + stopPropagation` (паттерн `showAddKindMenu`). Кнопка «🎞 +»
  (`doc-pull`) из `doc-tools` **удалена как дубль** — фрагмент добавляют ПКМ по
  окну блока или Alt+Enter; тоггл списка частей «🎞 n ▾/▸» остался.
  через `navigator.clipboard` + `execCommand("insertText")` (сохраняет Ctrl+Z,
  вызывает `input` → автосохранение). Буфер доступен только в secure-контексте
  (localhost/https) — при отсутствии `navigator.clipboard` тост-подсказка,
  Ctrl+C/V работают всегда. В ЗАГ/ПОДВ/ЗК и вне блоков — прежнее поведение
  (нативное меню / ПКМ-меню добавления).
- **Монитор в стиле Premiere Pro**: новый `.mon-head` (имя клипа + длительность;
  `setMonHead()` вызывается из loadVideo/loadedmetadata/смены fps — app.js),
  `.tc-line` удалена — `#curTc`/`#fileDur` переехали в транспортер; юникод-глифы
  кнопок заменены inline-SVG (иконки goIn/шаг/play-pause/loop/⏭; `#btnPlay`
  переключается классом `.playing`, а не textContent); скраббер — сплошная лента
  с синей зоной In–Out и синими скобками-ручками; палитра монитора — локальные
  CSS-переменные `--pr-*` (серые Premiere + синий акцент) строго внутри
  `.monitor`.   Все id/обработчики плеера (J/K/L, I/O, P, ручки, limitToMarks)
  сохранены без изменений.
- **Упрощение плеера (тот же день)**: убраны `.mon-head`, кнопки ±1 кадр
  In/Out (`nudgeMark` удалён), кнопка «⟳ P» (клавиша P и `togglePreview`
  остались), `playRate`/`volSlider` (всегда 1×/100%; ключи `ss_rate`/`ss_vol`
  больше не применяются — иначе пользователь не смог бы сбросить сохранённую
  скорость). Панель «В блок/как» (`targetBlock`/`sendMode`, `NEW_TARGET`,
  `ss_sendmode`) удалена: `#btnSend` живёт в транспортёре между `Out }` и `✕`
  и кидает фрагмент в **текущий блок** (куда курсор) через `pullPart`; `#curBlock`
  («→ Синхрон 2») — подпись цели слева от неё; `refreshTargets()` теперь обновляет
  только эту подпись (сигнатура/вызовы не менялись).
- **Удалены левой колонки и этапности (сессия 2026-09-13)**: из интерфейса
  убраны «МОИ ЗАДАНИЯ» (список/фильтр/поиск, `renderAssignments`, `storyMatches`,
  ПКМ-меню `showStoryMenu`), «ЛЕНТА» (`WIRE`/`renderWire` — статичная заглушка),
  бейдж статуса `#ocStatusBadge` и степпер `#ocStepper` (`renderStepper`,
  `advanceStage`, `statusLabel/statusCls/badgeText/badgeCls/STAGE_LABEL`), поле
  поиска в верхней шапке (`#ocGlobalSearch`). Переключение сюжетов — новое
  подменю «Проект» → «🎬 Сюжеты…» (`showStoriesMenu`, `.proj-menu.st-menu`:
  список с ✓/временем + дублировать/удалить). Вкладка «Утверждение» осталась
  как чек-лист выхода (4 экспорта, 5-я строка «Этап» убрана).
  **Модель данных не менялась**: `stage`/`stageDates`/`day`/`time` по-прежнему
  создаются, хранятся и ездят в черновиках (совместимость и правило §5.1) —
  просто нигде не рисуются. `OC.STAGES` жив (валидация импорта черновика).
- **Панели инструментов и тип блоков (13.09)**: `.doc-tools` теперь всегда видны
  (`opacity:.75`, на hover строки — 1); правило-исключение `.has-parts` удалено
  (в ЗК/ПОДВ/ЗАГ панель не показывалась до наведения). Кнопка **«⇄ сменить тип»
  удалена из всех блоков**: вместе с `showKindMenu`/`closeKindMenu`/
  `changeBlockKind` (вызовы `closeKindMenu()` из `showAddKindMenu`/
  `showBlockCtxMenu` убраны). Сменить тип существующего блока теперь нельзя —
  тип задаётся только при создании (ПКМ по пустому месту / `DOC_TRIGGERS`);
  в `doc-tools` остались 💬n, «🎞n ▾/▸» (фрагментные) и ✕.
- **Реквизиты в шапке сюжета (13.09)**: строка `ИД/СЛАГ/ИЗМЕНЁН` заменена на
  `КОРР ▾⚙ · ОПЕР ▾⚙ · МОНТ ▾⚙ · FPS · ТЕМП` + мелкий `ИД … · изм. …` справа.
  Вкладка «Авторы» (`#view-metadata`) и кнопка `#gnMetadata` **удалены** —
  `switchCenterTab` без ветки metadata; `renderHead()` больше не пишет
  `#ocStorySlug` (СЛАГ выведен из UI). Идентификаторы полей (`fioReporter/
  fioCam/fioEditor/fpsInput/readSpeed`, `REQ_IDS`, `data-names`) не менялись —
  вся логика saveReq/loadReq/имён работает как прежде; `st.req` по-прежнему
  хранится per-story. Стили: `.sh-meta .nf-row`, `.sh-num`, `.sh-id`;
  `.req-grid` удалён.
- **GUI: Graphite NLE + панельная архитектура (сессия 2026-09-14, К1–К5)**.
  Дизайн-язык по практикам DaVinci Resolve 19 / Premiere Pro 2024:
  - **К1 Токены**: палитра полностью переведена на нейтральный графит
    (`--bg-0 #1a1a1a`, `--bg-1 #232323`, `--bg-2 #2b2b2b`, `--input-bg`,
    `--ctl/--ctl-h`, `--line/--line-h`); единый акцент `--accent #4ea3ff`
    (`--pr-blue` монитора = он же, старого `--blue #147BEA` нет); второй
    акцент `--accent-2` (оранжевый) — предупреждения; `--ok/--warn/--err`;
    clip-цвета блоков в `--k-headline…--k-spiegel` (приглушённые,badge/kind-menu/
    mini-list берут их); `--r1/--r2`. Хардкод-цветов в CSS почти не осталось
    (кроме `--pr-*` монитора и чёрного/белого). Кнопки borderless-вида
    (рамка цвета фона), hover/active 120ms. Шкала шрифтов 10–14px (9/9.5/10.5/
    11.5/12.5/13.5 убраны), `--text3 #8a8a8a` — AA на графите. В JS удалены
    ссылки на несуществующие `var(--green)` (octopus, Rundown-таблица).
  - **К2 A11y + шпаргалка**: три группы табов получили `role=tablist/tab/
    tabpanel` + синхронизацию `aria-selected` в `switchCenterTab/
    switchMediaTab/renderCollab`; aria-labels иконочных кнопок; `<details>
    «Горячие клавиши»` из монитора удалён — вместо него модалка `#keysModal`
    (полный список) по клавише `?` (вне полей ввода) и кнопке `#ocHelp`;
    Esc/клик по фону закрывают.
  - **К3 Панели**: компонент `.panel-sec > .panel-head(26px)+.panel-body` c
    заголовками по-взрослому (нейминг B: рус.+латинизмы): **Монитор** (в шапке
    имя файла `#monFile`, пишет `loadVideo`), **Медиабраузер** (`#ocVidCount`,
    `#folderName`, view-toggle, `#btnRescan` переехали в шапку; старый
    `.mv-head` удалён), **SOT-лист**, **Графика**, **Approval**, **Notes**
    (бывш. collab). Кнопка `▾/▸` сворачивает body — состояние в localStorage
    `ss_fold` (JSON-карта, НЕ модель данных). Активная панель (pointerdown) —
    1px accent-линия сверху шапки. Таб «Обзор» → **«Rundown»** (id `view-summary`
    прежний). **Блоки = клипы PP**: левый цветной rail 4px (`border-left-color`
    по kind), бейдж 78→52px, длительность — белое mono в тёмной пилюле (зелёный
    в UI теперь только «OK»), фокус-блок — inset accent-рамка.
  - **К4 Статус-бар + сплиттер**: `footer#statusbar` (24px): `ИД · изм.`
    (переехали из `.sh-id`, id прежние), `FPS #sbFps` (зеркало `#fpsInput`),
    ИТОГО/план — `.total-row` перенесён из `#view-script` в бар (id `ocTotal`,
    `ocTotalPlan` и обработчик «план ✎» не менялись), индикатор сохранения
    `#sbSaved`: «…изменения» при `SS_HOOK.afterSave` → «✓ Ч:ММ сохранено» по
    `ocFlushNow`/`renderHead`. `div#vsplit` между колонками: drag меняет
    ширину rightcol (clamp 400px…70%), dblclick — сброс, ширина в `ss_rcw`.
    Мёртвые topnav-кнопки «Ресурсы/Планирование/Система» удалены. Дубль
    «Хронометраж: …» (`#totalDur`) из тулбара убран (строка в app.js
    защищена фолбэком).
  - **К5 Project Panel-практики**: красный «used-dot» у файлов/плиток, чей
    relPath встречается в `parts[]` блоков (`usedPathSet()/updateUsedDots()`
    в app.js; вызов — из `SS_HOOK.afterRender` и `refreshVideoList`);
    offline-строки фрагментов (путь пропал из просканированной папки) —
    `.doc-part.offline` красная штриховка + красноватое имя; selected-плитка —
    accent-outline.
  Id/ключи модели данных (`kind`, `stage`, `ss_req`, `oc_stories`…) не трогались.
  Проверки: `node --check`, id-кросс-дифф JS↔HTML, `./make-win.sh` перегенерирован.
- **GUI: интерактив §4 (сессия 2026-09-14, К6–К9)**.
  - **К6 Мини-таймлайн**: `#miniTl` (sticky под `#scriptToolbar`, `top:42px`),
    сегменты `.mt-seg` — ширина `flex-grow ∝ blockDur` (текстовые — оценка по
    ТЕМП), цвет `var(--k-*)`; клик → скролл+фокус (`mtJump`), drag → тот же
    `moveBlockTo` (`dragBlockId` общий с doc-block, `dragCleanup` чистит обе
    зоны). Пересборка — `updateMiniTl()` в конце `renderBlocks` и в
    `refreshDur` (живое обновление при наборе). Только view, данных не касается.
  - **К7 Drag клипа → блок**: элементы `#videoList` теперь `draggable`,
    `dragstart` кладёт `text/x-ss-file`=relPath; drop на `.has-parts`-блок →
    `addWholeFilePart(b, rel)` — целый файл `in:0, out:vf.dur||0` в конец
    `parts[]` (структура `{file,path,in,out}` не менялась), `histBefore()` →
    Ctrl+Z. Подсветка цели `.drop-file` (оранжевый accent-2). В обработчик
    плеерных клавиш app.js добавлен `if (mod) return;` — Ctrl/Meta больше не
    «съедаются» J/K/L/P.
  - **К8 Палитра Ctrl+K**: `#cmdPalette` (octopus.js): источники —
    `OC.stories` (переключение), блоки (`blockTitle` → `gotoBlock`),
    `PJ_ITEMS` (экспорты/файлы) + Поиск/Свернуть все/Шпаргалка. Фильтр —
    подстрока по «группа+имя+подсказка», до 50, ↑↓/Enter/Esc/mousedown
    (preventDefault — не теряет фокус). Открытие — keydown-слушатель в
    octopus (`/^(k|л)$/` + mod); Esc-цепочка расширена.
  - **К9 Undo-тосты + a11y-полировка**: `toast(msg, cls, ms, action)` —
    4-й аргумент `{label?, fn}` рисует кнопку, тост получает класс `.act`
    (pointer-events:auto), авто-мс 5000. Привязки: удаление блока → `doUndo()`;
    удаление сюжета → возврат снимка (с очисткой автозаглушки «Новый сюжет») +
    `loadStory`. `aria-current` в меню «Сюжеты…», `aria-label` у `#vsplit`.
  - Из §5 не делал: 4-pt spacing scale (отложить до рефактора отступов).
- **GUI: интернационализация + Octopus-бейджи (сессия 2026-09-14, Л1–Л4)**.
  Решение юзера: вариант B — **английский хром, русские тексты сообщений**.
  - **Л1**: реестр пиктограмм в app.js — `ICONS` (path-строки) + `icon(name,size)`
    + `badgeHtml(kind)`; ключи иконок = kind-ключи. `KIND_META.badge` →
    **HEADLINE / LEAD / VO / SOT / STANDUP / LIFE / SPIEGEL** (как в исходном
    OCTOPUS, см. git `a51f27e`); `.ru`-поля не тронуты — Word/CSV/MOGRT-экспорты
    и Fish Cutter не задеты. Бейдж — «soft-pill»: иконка+код, подложка
    `color-mix(--k-* 14%)` с rgba-фолбэком, текст `--kt-*` (новые light-токены,
    AA на тёмном), rail 4px — цвет-якорь. Тот же стиль в `.kind-item` (ПКМ-меню,
    добавлены подписи `.ki-ru`) и `.mi-badge` (SOT-лист).
  - **Л2 EN-хром**: табы `Script/Rundown/Sources/Social`, `Media/SOTs/Graphics/
    Approval`; навигация `Project|Story`; панели `Monitor / Media Browser /
    SOT List / Graphics / Approval / Notes`; поля шапки `REP/CAM/EDIT/FPS/PACE`;
    вкладки collab `COMMENTS/SUGGESTIONS/TASKS`; статус-бар `ID… · mod…`,
    `Total`, `plan …✎/set plan`, `✓ … saved / …unsaved`; `btnSend` = «Send →»;
    плейсхолдеры EN; `(SOT)`-суффикс спикера. **id/data-атрибуты/классы не менялись.**
  - **Л3**: SVG из `ICONS` в контролах: ⚙→gear, 💬→bubble, 🎞▾→film+chevron,
    ✕→x (del-блок, del-part, findbar, иконки модалок, btnClearMarks), ⤡все→
    fold+«All», стрелки findbar→chevrons, ✎ плана→pencil, ph-fold→chevron.
    Выпадающие меню («Проект», «Сюжеты…», ctx-меню) — эмодзи сохранены намеренно.
  - **Л4**: `DOC_TRIGGERS` += `head/nat/nats/toss/v-o` (RU-триггеры целы);
    шпаргалка «?» и README переписаны под EN-коды; в `CONTEXT` добавлен
    forward-note в раздел русификации 11.09.
  Проверено: `node --check`, id-дифф, `./make-win.sh`.
- **Титры: redesign + STANDUP (сессия 2026-09-14, M1–M4)**.
  - **M1**: титровая строка SOT/STANDUP перверстана в «пилюлю» `.doc-speaker`:
    иконка `person` (добавлена в `ICONS`) + `.b-speaker` (капс) + `.ts-sep` +
    `.b-role` + чип кода типа `.ts-code.sync/.standup`; `focus-within` —
    accent-рамка. Классы `.doc-speaker/.b-speaker/.b-role` сохранены
    (обработчики, guard «клик по свёрнутому»). Класс `.sot-suf` выведен.
    Позже в тот же день: чип `.ts-code` убран, пилюля — на всю ширину блока,
    `.b-role` = `flex:1` (должность тянется до края).
- **Шапка сюжета: папка вместо FPS/PACE (сессия 2026-09-14, N1–N3)**.
  Из `#storyHead` убраны ряды FPS/PACE — на их место встали `#btnFolderHead`
  («📁 Исходники» + переехавший туда `#folderName`; клик = всегда `pickFolder()`
  — выбор/смена пути; повторное открытие запомненной папки осталось за ⟳
  `btnRescan` в Media Browser — правка по фидбэку юзера: «умный» folderAction
  воспринимался как неработающий выбор) и `#btnStorySetup`
  (⚙). `#fpsInput`/`#readSpeed` **не удалены, а перенесены** в модалку
  `#setupModal` («Готово»/✕/Esc/фон) — id сохранены, поэтому `REQ_IDS`/saveReq/
  loadReq/`fpsVal`/`readCps`/зеркало `#sbFps`/черновики .json работают без
  правок. Глобальный keydown app.js игнорирует клавиши при открытой `setupModal`
  (guard рядом с `askModal`), Esc-цепочка octopus закрывает её. Пункт
  «📁 Папка исходников…» удалён из `PJ_ITEMS` (меню «Проект»); в Ctrl+K
  добавлены явные команды «Папка исходников…» (= pickFolder) и «Настройки сюжета».
  Попутно исправлен баг Л3: у трёх `.nf-gear` в index.html потерялась `>`
  открывающего тега (замена ⚙ на SVG съела скобку).

- **MOGRT: формат плагина LowerThirdsGenerator (15.09)**. `buildMogrtCsv()`
  переписан с 9-колоночного на **две колонки** `Имя Фамилия;Должность` (капс
  обеих, csv-экранирование `;"`, BOM, без `#`-строк — парсер плагина читает
  первую строку как заголовок). Строки = `TITR_KINDS` с непустым `speaker`
  в порядке сценария; `parseFio` упразднён; `exportMogrt()` без sep-аргумента;
  описания в PJ_ITEMS/Approval/README синхронизированы. Round-trip проверен
  тестом на копии парсера плагина (index.js: header-detect, quoted cells).  **Фикс (тот же день):** правки К3/К4 склеились — IIFE сплиттера и `applyFolds()`
  стартового уровня попали ВНУТРЬ `onclick` fold-кнопки (вставка по якорю
  `applyFolds();` нашла вызов внутри обработчика). Из-за этого слушатели `#vsplit`
  не навешивались до первого клика по ▾ (тяга границы «не работала») и свёрнутые
  панели не восстанавливались при загрузке. Обработчик закрыт корректно, IIFE и
  top-level `applyFolds()` вынесены наружу; во время drag добавлен
  `body.style.userSelect=none`; `#vsplit` расширен 5→7px.
  - **M2**: `const TITR_KINDS = new Set(["sync","standup"])` в app.js — единый
    предикат. Свёрнутый блок показывает `Имя Фамилия · Должность` (`.fs-titr`
    жирным) + excerpt текста; тултип мини-таймлайна и `blockTitle` для стендапа
    тоже с именем.
  - **M3**: стендап получил форму титров (плейсхолдер «Репортёр — ФИО»),
    слушатели input, перенос speaker/role при merge, поиск по спикеру/должности;
    Word-экспорт пишет «Стендап N. Имя, должность», импорт парсит это обратно
    (round-trip проверен regex-тестом); CSV Fish Cutter в колонке типа для
    стендапов дописывает speaker; обзор правок Word (`buildWordPlan`)
    отслеживает speaker/Changed для обоих типов.
  - **M4**: MOGRT = синхроны + стендапы с непустым ФИО (те же колонки, №
    сквозной по порядку появления); проверка «нет титров» и счётчик в тосте
    обновлены. SOT List остаётся списком синхронов (решение юзера).
  Модель данных не менялась (speaker/role хранились у всех блоков и раньше).

## 5. Правила при дальнейшей правке

1. **Не переименовывать** `kind`-ключи, `stage`-значения, `id` элементов, имена
   функций движка — на них завязаны сохранённые данные пользователей и миграции.
2. Локализация — только **видимые** подписи. С 14.09 хром (панели/табы/поля/
   статус-бар) на **английских NLE-терминах**, сообщения (тосты, диалоги,
   подсказки) — русские; `KIND_META.badge` — EN-коды, `.ru` — для экспортов.
3. Любое изменение в `js/`/`css/`/`index.html` → потом `./make-win.sh`.
4. Проверка синтаксиса: `node --check js/app.js js/octopus.js`,
   `python3 -m py_compile server.py`.
5. После деплоя — попросить юзера **Cmd+Shift+R** (жёсткая перезагрузка), т.к.
   старые index.html/js могли закэшироваться вразнобой.

## 6. Диагностика / восстановление данных юзера

- «Снимок данных» (кнопка в баннере или `window.OC`+IDB) → `ss-storage-dump.json`
  с `diag.idb.ok`, `diag.stories.count/titles`, фактом миграции.
- Сюжеты реально живут в **IndexedDB** `ss-studio/kv` (ключ `oc_stories`), а не в
  localStorage. localStorage теперь мелкий (`ss_req`, `ss_names_*`, `ss_view`…).
- Черновики юзера: `Проект → Сохранить…` → `.json` (блоки+реквизиты+fps+meta).

## 7. Что НЕ делалось (можно продолжить)

- ES-модули вместо двух классических скриптов (сейчас осознанно оставлено).
- Аватары/реальные пользователи (профиль — статичная заглушка «Иван Петров»).
- Разделы Ресурсы/Планирование/Система — заглушки (тост «в разработке»).
- Конкретные пункты изначального P0–P2 закрыты полностью.

## 8. История сессий opencode

Полный лог диалога развития — в opencode: `opencode session list` →
`opencode export <sessionID> > file.json` (для переноса в другую модель) или
`/share` (URL) / `opencode import`.
